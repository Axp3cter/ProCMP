# TODO

- __COMPOSER.loadFile and other file system features
- Make readme lest abstract
- bug: build date artifact macro works outside of a build frsme
- bug: pcmp init configs not being semantic/not workng
- refactor: prettify output json files for pcmp init, switch to a file based approach rather than in-code data
- fix the opening files only working for multi-open
- add more macros for useful data like getting current build varaint etc
- add script (luau) support and/or custom command support. Maybe they can use flags for certain build-time events, like pre and post (deploy, build, etc)
